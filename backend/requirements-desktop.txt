# Slim requirements for PyInstaller builds and CI.
# Excludes asyncpg (unused on desktop — SQLite only) and test infrastructure.
# Using this instead of requirements.txt prevents asyncpg from being importable
# in the PyInstaller scan environment, which would cause it to be bundled.
fastapi>=0.111.0
bcrypt>=4.0.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.30
greenlet>=3.0.0
alembic>=1.13.0
pydantic-settings>=2.2.0
httpx>=0.27.0
cryptography>=42.0.0
python-jose[cryptography]>=3.3.0
structlog>=24.1.0
aiosqlite>=0.20.0
aiofiles>=23.0.0
websockets>=12.0
boto3>=1.34.0

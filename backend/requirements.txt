fastapi>=0.111.0
jinja2>=3.1
bcrypt>=4.0.0
pyotp>=2.9,<3
segno>=1.6,<2
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.30
greenlet>=3.0.0
alembic>=1.13.0
pydantic-settings>=2.2.0
email-validator>=2.0.0
httpx>=0.27.0
cryptography>=42.0.0
python-jose[cryptography]>=3.3.0
structlog>=24.1.0
aiosqlite>=0.20.0
aiosmtplib>=3.0
asyncpg>=0.29.0
aiofiles>=23.0.0
websockets>=12.0
prometheus_client>=0.16.0
# SFBL-337: exact pins on boto3 family to short-circuit pip resolver
# backtracking.  aioboto3 transitively pins aiobotocore exactly, which then
# pins boto3 / botocore in narrow ranges.  Loose pins caused multi-minute
# resolution thrashing on every CI build (PR #88 and #89).  These exact
# versions resolve in O(1).  Bump together as a unit when upgrading.
boto3==1.40.61
aioboto3==15.5.0
aiobotocore==2.25.1
botocore==1.40.61
s3transfer==0.14.0
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-instrumentation-fastapi>=0.45b0
opentelemetry-instrumentation-httpx>=0.45b0
sentry-sdk[fastapi]>=2.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
# SFBL-334 / SFBL-342: Allure helpers (backend/tests/_allure_helpers.py).
# The reporter wiring (--alluredir= in CI pytest invocation) lands in
# SFBL-344 D; this entry is here from B because the helper module itself
# imports `allure` and needs the package installed to be testable.
allure-pytest>=2.13.0
anyio>=4.3.0
httpx[http2]>=0.27.0
moto[ses]>=5.0
aiosmtpd>=1.4

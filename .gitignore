# Environment (secrets, credentials)
.env
.env.*
!.env.example

# Python
venv/
.venv/
env/
__pycache__/

# Data (DB, input CSVs, output logs)
data/

# TLS certificates (private keys must never be committed)
certs/

# Node
node_modules/

# Electron build output — rebuilt locally, never committed
electron/dist/

# Electron build intermediates (iconset PNGs — only the compiled .icns is committed)
electron/build/icon.iconset/

# PyInstaller output — rebuilt by CI on every release
backend/dist/
backend/build/
backend/__pycache__/

# CDK
infrastructure/cdk.out/
infrastructure/cdk.context.json
infrastructure/dist/

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Editor / tools
.obsidian/

# Local test databases and scratch files
backend/test_*.db
runs.html

# Claude Code local state
.claude/

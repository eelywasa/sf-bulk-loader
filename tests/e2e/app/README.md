# app/ — Bulk-loader-specific

Playwright specs (tier-1a, tier-1b, tier-2), page objects, API setup helpers, and app-specific fixtures. May import from `sf/`; never the reverse (ESLint enforced). See D13 in `docs/specs/implemented/e2e-testing-spec.md`.

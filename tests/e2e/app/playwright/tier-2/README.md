# app/playwright/tier-2 — Scratch-org specs

State-mutating specs requiring a real scratch org: loads, query result writes, retry semantics, relationship-header assertions. Runs nightly + release-tag gate. Requires `E2E_SCRATCH_ORG`.

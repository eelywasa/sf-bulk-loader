# app/playwright/tier-1b — Metadata-reading specs

Specs for metadata-reading flows: mapping panel, object combobox, SOQL validator. Uses captured describe fixtures (`SF_DESCRIBE_FIXTURES_DIR`). Runs on every PR.

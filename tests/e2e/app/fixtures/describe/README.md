# app/fixtures/describe — App-specific describe fixture overlays

App-specific SObject describe overlays (e.g. Account.json with custom SFBL fields). First-match-wins against `sf/fixtures/describe/` per D5 overlay semantics. SFBL-321 may populate.

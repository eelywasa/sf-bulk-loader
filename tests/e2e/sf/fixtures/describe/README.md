# sf/fixtures/describe — Captured SObject describe fixtures (baseline)

JSON fixtures captured from a real scratch org: `_object_list.json`, `Account.json`, `Contact.json`, etc. SFBL-321 will populate. Used by Tier 1b via `SF_DESCRIBE_FIXTURES_DIR`.
